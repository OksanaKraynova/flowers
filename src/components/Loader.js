import React from 'react';

const Loader = () => {
    return (
        <>
            <img alt='Loading...' className='loader' src='/images/loader.gif' />
        </>
    );
};

export default Loader;